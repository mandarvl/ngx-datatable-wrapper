import { ModelDebouncedDirective } from './model-debounced.directive';

describe('ModelDebouncedDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelDebouncedDirective();
    expect(directive).toBeTruthy();
  });
});
